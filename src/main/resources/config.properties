# Log Collection Settings
log.watch.directory=logs
log.file.pattern=*.log

# Buffer and Batch Settings
log.buffer.size=1000
log.batch.size=100
log.batch.timeout.ms=5000

# Parser Settings
log.parser.default=json